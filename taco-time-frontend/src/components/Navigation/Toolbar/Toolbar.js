import React from 'react';

import classes from './Toolbar.module.css';

const toolbar = props => (
	<header className={classes.Toolbar}>
		<p>Time & Collaboration Organizer</p>
	</header>
);

export default toolbar;
